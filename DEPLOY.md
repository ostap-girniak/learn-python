# 🚀 Інструкція з деплою калькулятора

Ця інструкція допоможе вам задеплоїти калькулятор на Render.com, щоб він був доступний через інтернет.

## 📋 Передумови

1. ✅ Код запушений на GitHub
2. ✅ Створено акаунт на [Render.com](https://render.com) (безкоштовно)

## 🎯 Покрокова інструкція

### Крок 1: Підготовка GitHub

Переконайтеся, що всі файли запушені:
```bash
git add .
git commit -m "Готово до деплою"
git push
```

### Крок 2: Створення Web Service на Render

1. Відкрийте [Render Dashboard](https://dashboard.render.com)
2. Натисніть кнопку **"New +"** у верхньому правому куті
3. Виберіть **"Web Service"**

### Крок 3: Підключення GitHub

1. Якщо це перший раз, натисніть **"Connect GitHub"**
2. Дозвольте Render доступ до ваших репозиторіїв
3. Виберіть репозиторій `learn-python`
4. Натисніть **"Connect"**

### Крок 4: Налаштування деплою

Заповніть форму:

- **Name**: `calculator` (або будь-яка назва)
- **Region**: `Frankfurt` (найближчий до України)
- **Branch**: `main` (або ваша основна гілка)
- **Root Directory**: залиште порожнім
- **Environment**: `Python 3`
- **Build Command**: 
  ```
  pip install -r requirements.txt
  ```
- **Start Command**: 
  ```
  python -m gunicorn --bind 0.0.0.0:$PORT wsgi:app
  ```
  
  Або якщо не працює, спробуйте:
  ```
  gunicorn --bind 0.0.0.0:$PORT wsgi:app
  ```

### Крок 5: Створення сервісу

1. Перевірте всі налаштування
2. Натисніть **"Create Web Service"**
3. Render почне автоматичний деплой

### Крок 6: Очікування

- Деплой займе 2-5 хвилин
- Ви побачите логі процесу в реальному часі
- Після успішного деплою статус зміниться на **"Live"**

### Крок 7: Отримання URL

Після успішного деплою ви отримаєте URL типу:
```
https://calculator-xxxx.onrender.com
```

Цей URL буде доступний з будь-якого пристрою (комп'ютер, телефон, планшет)!

## 🔄 Автоматичний деплой

Render автоматично деплоїть ваш додаток при кожному push на GitHub. Просто:
1. Внесіть зміни
2. Зробіть `git push`
3. Render автоматично оновить додаток

## ⚙️ Налаштування (опціонально)

### Зміна назви URL

1. Перейдіть до налаштувань сервісу
2. Знайдіть **"Custom Domain"** або змініть назву сервісу
3. Збережіть зміни

### Environment Variables

Якщо потрібно додати змінні середовища:
1. Перейдіть до **"Environment"** в налаштуваннях
2. Додайте необхідні змінні
3. Збережіть та перезапустіть сервіс

## 🐛 Вирішення проблем

### Помилка "Build failed"
- Перевірте, чи всі залежності в `requirements.txt`
- Перевірте логі помилок у Render Dashboard

### Помилка "Application failed to start"
- Перевірте `Start Command` - має бути: `gunicorn --bind 0.0.0.0:$PORT wsgi:app`
- Перевірте, чи існує файл `wsgi.py`

### Додаток не відкривається
- Зачекайте 1-2 хвилини після деплою
- Перевірте статус сервісу в Dashboard
- Перевірте логі на наявність помилок

## 📱 Використання з телефону

Після деплою просто:
1. Відкрийте браузер на телефоні
2. Введіть URL вашого додатку
3. Користуйтесь калькулятором!

## 💡 Поради

- **Безкоштовний план Render** має обмеження: додаток "засинає" після 15 хвилин неактивності
- Перший запит після "сну" може зайняти 30-60 секунд (cold start)
- Для production використання розгляньте платний план

## 🎉 Готово!

Тепер ваш калькулятор доступний в інтернеті! Поділіться URL з друзями та родиною.
